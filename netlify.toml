[build]
  command = "npm i --legacy-peer-deps --force && npm run build"
  publish = "dist/browser"

[build.environment]
  NODE_VERSION = "16"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
